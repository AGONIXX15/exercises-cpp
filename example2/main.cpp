#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(nullptr);
  ios::sync_with_stdio(0);

  vector<int> v = {1, 2, 3, 4, 5, -1};

  int index = 3;

  // for (int i = v.size() - 1; i >= index; --i) {
  //   swap(v[i], v[i - 1]);
  // }
  //
  // for (auto value : v)
  //   cout << value << ' ';
  //
  int value = 2;
  int a = value--;
  cout << a << '\n';
}
