#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(nullptr);
  ios::sync_with_stdio(0);
  size_t t; cin >> t;
  while(t--) {
    size_t n,m; cin >> n >> m; 


    unordered_set<size_t> set;
    for(size_t i = 0; i < n; ++i) {
      size_t s; cin >> s;
      vector<size_t> v(s);
      for(auto& val: v) cin >> val;
      set.insert()
    }
  }

  return 0;
}
