#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main() {
  cin.tie(nullptr);
  ios::sync_with_stdio(0);
  
  size_t t; cin >> t;
  while(t--) {
    
    ll x,y; cin >> x >> y;
  }
  return 0;
}
