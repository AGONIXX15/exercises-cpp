#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(nullptr);
  size_t t; cin >> t;
  int max_operations = 1000;
  while(t--) {
    int n,k; cin >> n >> k;
    vector<int> cap(k);
    for(auto& val: cap) cin >> val;
    vector<int> initial_levels(n);
    for(auto& val: initial_levels) cin >> val;


  }
  return 0;
}
