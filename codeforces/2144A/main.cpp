#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(nullptr);
  ios::sync_with_stdio(0);

  size_t t; cin >> t;
  long long m = 998244353;
  while(t--) {
    size_t n; cin >> n;
    vector<int> v1(n);
    vector<int> v2(n);
    for(auto& val: v1)  cin >> val;
    for(auto& val: v2)  cin >> val;

    
  }

  return 0;
}
