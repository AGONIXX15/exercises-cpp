#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(nullptr);
  size_t t; cin >> t;
  while(t--) {
    int n,k,p,m; cin >> n >> k >> p >> m;
    vector<int> cost_cards(n);
    for(auto& val: cost_cards) cin >> val;
    

  }
  return 0;
}
